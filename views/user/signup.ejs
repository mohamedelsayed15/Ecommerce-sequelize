<%- include('../includes/head.ejs') %>

    <link rel="stylesheet"  href="/css/login&signup.css">
</head>
<body>
    <%- include('../includes/nav.ejs') %>
        <div class="container">

        <form method="post" action="/user/create-user" novalidate>
            <label for="name">Name</label>
                <br>
                <input type="name" autocomplete="off" name="name" id="name" value="<%= pastInput.name %>" required>
                <br>
                <label for="email">Email</label>
                <br>
                <input type="email" name="email"  id="email" value="<%= pastInput.email %>" required>
                <br>
                <label for="password">Password</label>
                <br>
                <input type="password" name="password" id="password" value="<%= pastInput.password %>" required>
                <br>
                <label for="confirmPassword">Confirm password</label>
                <br>
                <input type="password" name="confirmPassword" id="password" value="<%= pastInput.confirmPassword %>" required>
                <br>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button class="button" id="signup" type="submit">SIGN UP</button>
                <div class="links" >Already have an account ? <a href="/user/login-user">Sign In</a></div>
        </form>
        <% if(errorMessage){ %>
            <div id ="error"> <%= errorMessage %></div>
            <% } %>
    </div>
    <%- include('../includes/footer.ejs') %>
