<%- include('../includes/head.ejs') %>

    <link rel="stylesheet"  href="/css/login&signup.css">

</head> 
    <body>
        <%- include('../includes/nav.ejs') %>
        
    <div class="container">
        <form method="post" action="/user/reset-password" novalidate>
                <label for="password">Password</label>
                <br>
                <input type="password"  name="password" value="<%= pastInput.password %>" required>
                <br>
                <label for="confirmpassword">Confirm Password</label>
                <br>
                <input type="password"  name="confirmPassword" value="<%= pastInput.confirmPassword %>" required>
                <br>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <input type="hidden" name="id" value="<%= userId %>">
                <input type="hidden" name="token" value="<%= token %>">
                <br>
                <button class="button"  id="reset" type="submit" >Reset Password</button>
        </form>
        <% if(errorMessage){ %>
            <div id ="error"> <%= errorMessage %></div>
            <% } %>
    </div>
    <%- include('../includes/footer.ejs') %>
