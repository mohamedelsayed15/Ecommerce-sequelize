<%- include('includes/head.ejs') %>
<link rel="stylesheet"  href="/css/browse-product.css">

</head>
    <body>
        <%- include('includes/nav.ejs') %>
        <main class="centered">
            <div class="container">
            <h1><%= product.title %></h1>
            <hr>
            <br>
            <div>
                <img src="<%= product.image_url %>" alt="<%= product.title %>">
            </div>
            <h2>$<%= product.price %></h2>
            <p><%= product.description %></p>
            <form action="/shop/cart/add-to-cart/<%= product.id %>" method="post">
                <button class="button" type="submit">Add to Cart</button>
            </form>
        </div>
        </main>
        <%- include('includes/footer.ejs') %>